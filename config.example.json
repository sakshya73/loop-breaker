{
  "_comment": "Copy to ~/.claude/loop-breaker/config.json (global) or <project>/.loop-breaker.json (project). All keys optional; omitted keys use defaults. Values are type-checked and clamped, so a typo (e.g. a quoted number) can never disable detection.",

  "mode": "kill",

  "consecutive_threshold": 5,

  "structural_detection": true,

  "cycle_reps": 4,
  "cycle_max_period": 6,
  "read_only_cycle_exempt": true,

  "window_size": 0,
  "window_threshold": 0,

  "max_tool_calls": 0,
  "max_estimated_tokens": 0,

  "ignore_tools": [],
  "history_size": 60
}
